---
interface Props {
  googleApiKey: string;
  tracks?: { url: string; label: string }[];
  landmarks?: { url: string; label?: string }[];
  autoTracking?: boolean;
}
const { googleApiKey, tracks = [], landmarks = [], autoTracking = false } = Astro.props;
---
<igc-viewer google-api-key={googleApiKey} tracks={JSON.stringify(tracks)} landmarks={JSON.stringify(landmarks)} auto-tracking={autoTracking}></igc-viewer>
<script>
  import '../../IGCViewer';
</script>
